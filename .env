NODE_ENV=development
API_PREFIX=/api
PORT=3000
OPENAPI_URL=/openapi/json

# 非敏感默认值（本地可按需覆盖）
DIALECT=postgresql
DATABASE_URL=postgresql://127.0.0.1:5432/yuumi

# 可选默认值
LOG_FILE_DIR=logs
IDEMPOTENCY_TTL_MS=86400000

# 必填（敏感信息）：请在本地自行填写，不提交真实值
# JWT_SECRET=replace-with-a-strong-secret
